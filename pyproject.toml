[build-system]
requires = ['setuptools>=61.0']
build-backend = 'setuptools.build_meta'

[project]
name = 'quant-alpha-lab'
version = '0.1.0'
requires-python = '>=3.11'

[tool.setuptools]
packages = ['src', 'src.features', 'src.utils', 'config']